# <Recipe Name>

**One-sentence description of the problem this recipe solves.**

## Use this when

- Bullet describing a customer signal / scenario where this is the right choice
- Another bullet — what makes this recipe different from alternatives
- (Optional) When NOT to use this

## Results / validation

If you have benchmark numbers, scaling results, or cost data, put them here.
Customers read this before they read the instructions.

| Metric | Value |
|---|---|
| e.g., Training throughput | X tokens/sec on Y instance |

## Prerequisites

- AWS account with access to <services>
- IAM permissions: <list>
- Tools: AWS CLI, Python 3.x, <other>
- API keys: <if any>

## Environment variables

```bash
export AWS_REGION=us-west-2
export S3_BUCKET=...
```

## Run it

```bash
pip install -r requirements.txt
python <entrypoint>.py
```

Expected output / next steps:

- ...

## Architecture

Optional diagram or link to `ARCHITECTURE.md` for complex recipes.

## License

MIT (or inherit from repo root).
